# SPDX-License-Identifier: Apache-2.0
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: gide
networking:
  # Rootless podman cannot load ip_tables kernel module.
  # nftables proxy mode works without it (requires Kubernetes >= 1.29).
  kubeProxyMode: "nftables"
nodes:
  - role: control-plane
